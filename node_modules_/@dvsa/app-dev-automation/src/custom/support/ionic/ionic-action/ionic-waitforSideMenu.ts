import { getElementByReference } from '../../../../helpers/element-reference-helper';
import waitUntilElementStopsMoving from '../../lib/waitUntilElementStopsMoving';

/**
 * Perform an click action on the given element
 * @param  {String}   selector Element selector
 */
export default async (
  menuSelector: string,
  // eslint-disable-next-line no-unused-vars
  showhide: string,
) => {
  /**
     * The method to call on the browser object
     * @type {String}
     */

  const elementSelector = getElementByReference(menuSelector);

  await waitUntilElementStopsMoving(elementSelector);
};
