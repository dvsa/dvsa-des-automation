import { getElementByReference } from '../../../helpers/element-reference-helper';

/**
 * Clear a given input field (placeholder for WDIO's clearElement)
 * @param  {String}   selector Element selector
 */
export default async (selector: string) => {
    const elementSelector: string = getElementByReference(selector);
    await $(elementSelector).clearValue();
};
